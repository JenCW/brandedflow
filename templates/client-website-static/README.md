# Client Website Template — Static (Brochure)

Use this when a client needs:
- fast brochure site
- minimal dependencies
- strong SEO + performance

This template focuses on consistent structure and lead capture conventions (client_id + UTMs).

## How to use
1. Copy this folder into: `clients/<client>/04_website/`
2. Edit `index.html` content + styling
3. Deploy to Netlify

## Lead capture
This starter uses Netlify Forms by default. Add hidden inputs for:
- `client_id`
- `lead_type`
- UTM fields (capture via query string and set hidden inputs if desired)

